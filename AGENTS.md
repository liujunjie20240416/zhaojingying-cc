## Agent skills

### Issue tracker

任务与 PRD 保存在本地 `.scratch/` Markdown 文件中。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认状态：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单一上下文布局。详见 `docs/agents/domain.md`。
